import { Guides } from '@/components/Guides'
import { Resources } from '@/components/Resources'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'Spark Made Easy',
  description:
    'The fastest way to learn Spark.',
}

<HeroPattern />

# ⚡ Spark Made Easy

[Apache Spark](https://spark.apache.org/) is a simple, massively scalable, data processing engine... and you should use it. {{ className: 'lead mt-10' }}

For most common data tasks, it's also probably simpler than using [Pandas](https://pandas.pydata.org/) or [NumPy](https://numpy.org/).
And if you're already comfortable with SQL, you can learn about 80% of what you'll probably ever need in a single afternoon. {{ className: 'lead' }}

<div className="not-prose">
  <Button href="/quick_start" variant="text" arrow="right">
    So let's get started
  </Button>
</div>
