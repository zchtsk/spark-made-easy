export const metadata = {
  title: 'Hello World',
  description:
    'Create a spark context and print hello world',
}

# Hello World

One nice thing about Spark, is that under the hood, it's basically just a SQL dialect. {{ className: 'lead' }}
<Row>
    <Col>
    First create a spark context.{{ className: 'pt-28 text-right' }}

    You can use `sql()` to write raw sql queries. {{ className: 'text-right -mt-1' }}

    Use can use `show()` to print your dataframe. {{ className: 'text-right -mt-1' }}
    </Col>
    <Col>
    ```python {{ title: "PySpark" }}
    from pyspark.sql import SparkSession

    spark = SparkSession.builder.getOrCreate()

    df = spark.sql("SELECT 'Hello World'")

    df.show()
    ```

    ```python {{ title: "output" }}
    +-----------+
    |        _c0|
    +-----------+
    |Hello World|
    +-----------+
    ```
    </Col>
</Row>